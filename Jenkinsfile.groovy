pipeline {

    steps {
        stages {
            stage('Checkout scm') {
                checkout scm
            }
            stage('Test') {
                steps {
                    echo 'Testing..'
                }
            }
            stage('Deploy') {
                steps {
                    echo 'Deploying....'
                }
            }
        }
    }
}